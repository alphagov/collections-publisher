<% if step.broken_links? %>
  <span class="govuk-tag govuk-tag--warning">
    <%= step.broken_links.count %> broken link<%= "(s)" if step.broken_links.count > 1 %> found
  </span>
<% elsif step.link_report? %>
  <span class="govuk-tag govuk-tag--success">
    No broken links
  </span>
<% end %>

<% if resource.errors.any? %>
  <%= render "govuk_publishing_components/components/error_summary", {
    title: "There is a problem",
    items: resource.errors.full_messages.each.map do |message|
      {
        text: message
      }
    end
  } %>
<% end %>

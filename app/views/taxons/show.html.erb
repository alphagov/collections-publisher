<%= header taxon_form.title, breadcrumbs: [:taxons, taxon_form] %>

<table class="tags-list">
<tr>
  <th>Parent taxons</th>
  <th>Base path</th>
</tr>
<% taxon_parents.each do |taxon_parent| %>
  <tr>
    <td><%= link_to taxon_parent['title'], taxon_path(taxon_parent['content_id']) %></td>
    <td><%= link_to taxon_parent['base_path'], "#{Plek.new.find('www') + '/api/content' + taxon_parent['base_path']}" %></td>
  </tr>
<% end %>
</table>

<table class="tags-list">
<tr>
  <th>Title</th>
  <th>URL</th>
</tr>
<% tagged.each do |content_item| %>
  <tr>
    <td><%= content_item["title"] %></td>
    <% content_item_url = Plek.new.website_root + content_item["base_path"] %>
    <td><%= link_to content_item["base_path"], content_item_url %></td>
  </tr>
<% end %>
</table>
